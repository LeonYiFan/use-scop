interface:
  display_name: "Use scop"
  short_description: "Single-cell analysis with scop"
  default_prompt: "Use $use-scop to perform single-cell analysis and visualization with scop. Prefer the installed scop package, check whether scop is installed before assuming runnable code, and only use source inspection when explicitly needed."
policy:
  allow_implicit_invocation: false
